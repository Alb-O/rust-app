# Rust App Test Instructions

- Keep command behavior explicit and predictable.
- Preserve stable JSON output fields for downstream consumers.
- Prefer simple, readable CLI flow over abstract layering.
