.devenv/
.direnv/
target/
result

# `devenv.lock` is ignored for fast local-first polyrepo workflow without diff churn.
# `Cargo.lock` is tracked for reproducible binary builds and downstream source imports.
devenv.lock

*.local.*
*.override.*
