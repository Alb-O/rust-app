.devenv/
.direnv/
target/
result

# Lockfiles ignored for fast local-first polyrepo workflow without diff churn
# Manually `git add -f *.lock` when actually publishing
devenv.lock
Cargo.lock

*.local.*
*.override.*
